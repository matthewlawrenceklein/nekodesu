<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-12 px-4">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow-lg p-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-6">Generate New Dialogues</h1>

      <p class="text-gray-600 mb-8">
        Create personalized Japanese dialogues based on your WaniKani vocabulary.
        Each dialogue includes 10 comprehension questions (4 random questions will be shown per attempt).
      </p>

      <%= form_with url: generate_dialogues_path, method: :post, class: "space-y-6" do |f| %>
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">
            Difficulty Level
          </label>
          <select name="difficulty_level" class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:outline-none">
            <option value="beginner">Beginner (JLPT N5 - Levels 1-10)</option>
            <option value="intermediate">Intermediate (JLPT N4-N3 - Levels 11-30)</option>
            <option value="advanced">Advanced (JLPT N2-N1 - Levels 31-60)</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">
            Number of Dialogues
          </label>
          <input type="number" name="count" value="5" min="1" max="20" class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-blue-500 focus:outline-none">
          <p class="text-sm text-gray-500 mt-1">Maximum 20 dialogues per generation</p>
        </div>

        <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded">
          <p class="text-sm text-yellow-800">
            ⚠️ This will run synchronously and may take 1-2 minutes per dialogue.
            Please don't close this page while generation is in progress.
          </p>
        </div>

        <div class="flex space-x-4">
          <%= f.submit "Generate Dialogues", class: "flex-1 bg-blue-600 hover:bg-blue-700 text-white font-bold text-lg py-4 rounded-lg transition duration-200 cursor-pointer" %>
          <%= link_to "Cancel", root_path, class: "flex-1 bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold text-lg py-4 rounded-lg transition duration-200 text-center" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
